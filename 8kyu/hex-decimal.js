// Complete the function which converts hex number(given as a string) to a decimal number.

function hexToDecimal(hexToString){
    return parseInt(hexToString, 16)
    
}
console.log(hexToDecimal("baa"))